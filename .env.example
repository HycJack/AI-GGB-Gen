# The app reads no API key from the environment. It is entered at runtime in the
# gear-icon settings dialog and kept in localStorage under
# "geogebra_tutor_api_config" (see src/lib/storage.ts).

# DISABLE_HMR=true  — disables Vite HMR/file watching.
# AI Studio sets this automatically so agent edits don't cause flicker.
# DISABLE_HMR=true
